# Dawn - ルーチントラッカー

## 概要

ルーチンの継続をサポートするCLI。Streak（連続記録）で継続状況を可視化し、モチベーション維持を助ける。

## 技術スタック

- 言語: Go
- データ形式: JSON
- 保存先: `~/.config/dawn/`

## コマンド

### `dawn [ルーチン内容]`

ルーチンを記録する。

- 引数にルーチン内容を渡すとそのまま記録
- 引数なしの場合は対話式でルーチン内容を入力

```bash
# 引数で渡す
dawn "読書"

# 対話式
dawn
# → 何をしましたか？: 読書
```

### `dawn log`

継続状況を表示する。

```
🔥 Current Streak:  12 days
🏆 Longest Streak:  34 days
📊 Streak Sum:      127 days
```

- **Current Streak**: 今日から遡って連続で記録が続いている日数
- **Longest Streak**: 過去全期間で連続記録が最大だった日数
- **Streak Sum**: 合計実行回数（通算記録日数）

### `dawn history`

過去の記録を一覧表示する。

```
2024-01-15 06:32  読書
2024-01-14 05:58  ランニング
2024-01-13 06:15  英語学習
```

### `dawn edit`

JSONファイルを直接編集する。

- `$EDITOR`環境変数で指定されたエディタを起動
- 未設定の場合はデフォルトエディタ（vim等）を使用

### `dawn help`

ヘルプを表示する。

## 制約

- 記録可能時間: **5:00〜8:00のみ**
- 1日1回のみ記録可能

## データ構造

### 保存先

```
~/.config/dawn/
└── data.json
```

### JSONスキーマ

```json
{
  "records": [
    {
      "date": "2024-01-15",
      "time": "06:32",
      "routine": "読書"
    }
  ]
}
```

## メッセージ

| 状況 | メッセージ |
|------|-----------|
| 記録成功 | `🌅 また一日、生き延びたな` |
| 時間外（5:00前） | `🚬 夜型の人間には用はない` |
| 時間外（8:00以降） | `🚬 朝は待ってくれない` |
| 記録済み | `🚬 欲張るな、1日1回だ` |